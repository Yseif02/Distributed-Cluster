# Distributed-Cluster
Scalable, fault-tolerant distributed master–worker cluster for serverless Java execution. Implements ZooKeeper-style leader election, gossip-based heartbeats, and automatic recovery from leader and worker failures, with a gateway providing a stable HTTP interface.
